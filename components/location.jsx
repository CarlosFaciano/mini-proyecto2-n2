/* 
import WeatherApi from "../api/weatherFetch1/route"
import { useState } from "react"



  function UserLocation() {
 
  

         if (navigator.geolocation) {
             
             const data = navigator.geolocation.getCurrentPosition(ShowPosition)
             
          }  
        
       
      /* return(
      {/* <WeatherApi
       latitude = {latitude}
        longitude = {longitude}

      /> */
    

  /*  function ShowPosition (pos, { setLatitude, setLongitude} ){
      const lat = position.coords.latitude
      const lon = position.coords.longitude
      
     /*  setLatitude(lat) 
      setLongitude(lon) */
      
      
   
  
